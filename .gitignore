# ── Build output & runtime data ────────────────────────────────────────────
build/bin/
data/
SampleData/
*.exe
*.syso

# SQLite runtime artifacts (also produced by ad-hoc tests outside data/)
*.db
*.db-shm
*.db-wal
*.sqlite
*.sqlite3

# ── Go ─────────────────────────────────────────────────────────────────────
*.test
coverage.out
GOCACHE/
go-build/
go-cache/
go.work
go.work.sum
.gocache/
/vendor/

# ── Frontend (Deno + Vite) ─────────────────────────────────────────────────
node_modules/
*.tsbuildinfo
coverage/
frontend/.vite/
frontend/.cache/
frontend/dist/*
!frontend/dist/.gitkeep
frontend/package.json.md5
frontend-legacy/.vite/
frontend-legacy/.cache/
frontend-legacy/dist/*

# npm/yarn/pnpm lockfiles — this project is Deno-only (deno.lock is committed)
package-lock.json
pnpm-lock.yaml
yarn.lock

# Linter caches
.eslintcache
.stylelintcache

# ── Wails ──────────────────────────────────────────────────────────────────
wailsdev.log
.wails-scaffold/
.vue-scaffold/

# ── Local caches & environment ─────────────────────────────────────────────
.cache/
.deepseek/
.env
.env.local
.env.*.local
*.log
*.tmp
.tmp/
tmp/

# ── Agent / tooling scratch ────────────────────────────────────────────────
.claude/
.agent/
.agents/
.agent-team/
.superpowers/
mcps/
AGENTS.md
# CLAUDE.md is tracked on purpose — do not ignore it.

# Console dumps left behind by typecheck/build runs
frontend/*_out.txt
frontend/*_fresh.txt
frontend-legacy/*_out.txt
frontend-legacy/*_fresh.txt

# Local helper scripts (transient — do not commit)
_*.bat

# Handoff documents
docs/handoff/

# ── Editors & OS ───────────────────────────────────────────────────────────
.vscode/
.idea/
.vs/
*.swp
*~
.DS_Store
Thumbs.db
ehthumbs.db
desktop.ini

# ── Merge / patch residue ──────────────────────────────────────────────────
*.orig
*.rej

# ── Local accidental artifacts (shell redirection & flag typos on Windows) ──
$null
-Force/
env/
/go/
