node_modules/
node_modules/.tmp/
dist/
dist-ssr/
.vite/
.cache/
coverage/
test-results/
blob-report/
playwright-report/
.vite-inspect/
*.local
*.tsbuildinfo
package.json.md5
package-lock.json
pnpm-lock.yaml
yarn.lock
.eslintcache
.stylelintcache

# Wails bindings: only generated .ts is committed. The bare `wails3 generate
# bindings` (without -ts) emits .js next to it — keep that out of the tree.
bindings/**/*.js
