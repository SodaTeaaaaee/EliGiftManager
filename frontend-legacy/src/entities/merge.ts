/**
 * Merge entity types — re-exported from generated Wails DTO.
 *
 * The generated `dto.MergeProfilesResult` class (wailsjs/go/models.ts) is the
 * authoritative definition. This file re-exports it as a type alias so that
 * existing `import type { MergeProfilesResult } from '@/entities/merge'`
 * continues to work.
 */
import type { dto } from '@/../wailsjs/go/models'

export type MergeProfilesResult = dto.MergeProfilesResult
