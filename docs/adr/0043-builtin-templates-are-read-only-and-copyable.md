# 内置模板只读且可复制

哔哩哔哩和柔造等内置模板只读，随软件升级更新：启动时按（平台、文档类型、方向）幂等落库，内容变化时原地覆盖并递增版本。用户可以复制内置模板形成自己的版本，用户模板独立版本化，不被升级覆盖。

内置模板是目录，不是可直接使用的配置：导入、导出和回写只接受用户（活动）模板，传入内置模板会被拒绝，某个（平台、方向、文档类型）没有活动模板时操作以明确错误失败，而不是静默退回代码默认值。内置模板可以直接用样例文件测试，复制之前先试是允许的。
